services:
  app:
    build:
      target: runtime
    environment:
      NODE_CLUSTER: false
    command:
      [
        "sh",
        "-c",
        "export DOTENV_PRIVATE_KEY_PRODUCTION=\"$(tr -d '\\r\\n' </run/secrets/dotenv_private_key_production)\"; exec bun --no-env-file dist/treaty/server/server.mjs",
      ]
